apiVersion: v1
kind: Service
metadata:
  name: {{ include "currency-converter.fullname" . }}
  labels:
    {{- include "currency-converter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "currency-converter.selectorLabels" . | nindent 4 }}